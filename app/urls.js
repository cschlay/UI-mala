/**
 * Keeps all urls in one place.
 *
 * Versioning API is forbidden.
 */

const apiUrls = {
  login: "/login",
  githubOAuth: "/github-login",
};

const urls = {
  login: "/login",
};

export { apiUrls, urls };
