const testInterfaceUrl = "/tests";

/**
 * Resets our test user, should only be run once in the beginning because of async tests.
 */
// eslint-disable-next-line fp/no-nil
const resetTestUser = () => {};

export { resetTestUser };
